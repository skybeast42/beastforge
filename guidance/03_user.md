<!--
INSTANCE FILE — how to work with THIS user (preferences, working style).
Ships EMPTY: do not preload. CC fills it as it learns the user — durable,
cross-chat working preferences only, NOT a session log. Read every session.
This is the in-repo home that replaces internal memory for user-learning;
when unsure whether something belongs here, ask.
-->

# User — working preferences

Status: empty — no preferences recorded yet.

_CC appends durable, cross-chat working preferences here as they surface
(e.g. language register, design taste, collaboration style, privacy
boundaries, numbering preference)._

**Triggers for capture** (propose, don't silently write):

- After any explicit User feedback (correction, preference, *"do X / don't do X"*).
- After any non-obvious User choice (when the User picks a path you wouldn't have predicted).
- At session-end boundaries (per the Persistence *"close at boundaries, proactively"* rule).

_Announce the entry; ask when unclear. Keep entries terse — this is
preferences, not a session log. Composes with the rest of `guidance/`;
never lowers `01_standing.md`'s floor._
