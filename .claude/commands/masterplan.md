---
description: Shape MASTERPLAN.md — formulate the strategy and a clear conceptual (un-numbered) step sequence
---

Work on `MASTERPLAN.md` with the User, in **planning mode**.

1. Read `CLAUDE.md` (rules), the current `MASTERPLAN.md`, and `STATE.md`.
2. Help the User formulate it well — sharpen Mission, Scope-in, Scope-out, Constraints. Propose wording; ask focused questions only where genuinely ambiguous. Don't interrogate.
3. Structure the work as a **conceptual sequence of steps — described, not numbered.** MASTERPLAN is the planning surface; the filesystem task numbers (`work/TASK_NNN_<name>/`) are execution accounting and drift from this list deliberately. **Do not assign numbers here.**
4. Keep it **durable**: anything that will change every session belongs in `STATE.md`, not here — move such content out.
5. **Non-destructive**: propose the revised `MASTERPLAN.md` for the User's approval *before* writing. Flip `Status:` to `active` once it has real content.
6. **Insight capture** — propose adding an *Active insight captures* section to `guidance/02_derived.md` listing the active capture **types** AND explicit **when-to-write triggers**. **Always offer template/meta-feedback capture** (a `[TEMPLATE]` type → `tracks/00_insights/template.md`) — a self-improving template captures feedback about itself *regardless of publication scope*. **Additionally**, if MASTERPLAN's Scope-in names publication, offer those publication types too (→ `tracks/00_insights/<type>.md`, consumed by `/deck`; other publication forms by asking CC from the same harvest — no command). Triggers: *a surprise lands; a non-obvious workaround; a decision with hidden depth; a task-closing gotcha* — **plus a cadence trigger** for long repetitive work (*checkpoint every ~K items / sub-block*, since event triggers go silent in steady-state loops). Default entry format: *date · type/tags · one-line title · observation · why-it-matters*. Propose, don't impose.

After this, `/resume` maps the conceptual steps to the next task brief.
