---
description: Show the project's glossary, rules, and command index (a view of CLAUDE.md)
---

Read `CLAUDE.md` and present a **concise** rendered summary for the User — not the whole file. Include:

1. **Actors** — User / CC in one line each.
2. **Glossary** — task, brief, ticket; task self-containment (`TASK_NNN/` + the `00_Briefs…05_Reports` stage sequence); the `00_` reservation; the folder-per-script output naming (`04_Output/<NN_script>/<artifact>.ext`).
3. **The model** — the spine (global memory + governance) · the homes (`work/` task-first · `apps/` · `tracks/` · `background/` · `guidance/`) · hidden=machinery, and the per-task reproducibility invariant.
4. **Command index** — the table from CLAUDE.md, grouped by bucket (lifecycle · structure · capture · reference · environment · bootstrap).

Keep it tight and scannable — this reduces hidden rules, so favour clarity over completeness. Point to `CLAUDE.md` for the authoritative detail.
