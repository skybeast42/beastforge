<!-- TEMPLATE_CHANGELOG — SHIPPED SUMMARY. **HEADLINES ONLY**: one line per version (no dates, no detail),
newest on top, + **ONE consolidated migration block** whose target is the word **"current"** (NEVER a
hardcoded version number). The complete, granular per-version log is the maintainer's
`tracks/template_changelog_full.md` (NOT shipped) — `/reforge` prefers it when the source carries it, else
uses the migration below. A field project inherits this file and may keep its own CHANGELOG.md for its
deliverable. See CLAUDE.md / DEC 119.
**Do NOT re-expand the headlines into detail, and do NOT stamp the migration with a version number — both
are recurring mistakes: detail lives only in the full log; the migration target is always "current".** -->

# TEMPLATE_CHANGELOG

## Versions — headlines (newest first; full detail is in the maintainer's full log)

* 3.2.5 — AIterritory ASSESS naming: script-mirrored companion (`ASSESS_<scriptnum>_<desc>`) + `ASSESS_ADD_NN` ad-hoc namespace.
* 3.2.4 — `/tokens` append-only time-sorted ledger + `/ground` keep-`distilled.md`-general guard + relay-message skeleton asset.
* 3.2.3 — public README overhaul (multi-persona audit) + asks-relay self-bootstrap (no manual `/habitat`).
* 3.2.2 — operator inbox: read & reply through any beast (+ `/loop` autonomy, `.fleetignore` placement note).
* 3.2.1 — co-ignored siblings = mutually isolated (fleet-model test).
* 3.2.0 — fleet model simplified to default-open + `.fleetignore` (`/unfleet` removed); README inter-beast-comms section.
* 3.1.0 — asks-relay hardening (relay/multi-beast audit resolution; + AIterritory gap-check exemption).
* 3.0.2 — multi-beast awareness (a beast runs only while its own session is open).
* 3.0.1 — asks-relay loop-guard sharpened + parameterized (`relay.max_hops`).
* 3.0.0 — inter-beast asks relay (`.habitat/asks/` + `/inbox`); `ASK.md` retired.

## Migration — any 2.2.0 or later → current

**Generic overwrites (idempotent).** Overwrite the template-generic files from the source payload —
`CLAUDE.md`, `.claude/commands/*`, `guidance/01_standing.md`, `.templates/*`, `LICENSE`, and this changelog.
That carries every doc/spec/behavior change in the versions above: the fleet-model rework, the relay
hardening + its schema (`goal`/`ask_repo`/`ask_commit`; thread-state **derived**, no stored `status` — old
messages still reconcile), the operator-inbox + the **self-bootstrapping relay** (filing a message
auto-creates the habitat + inbox), and the README. **Then DELETE `.claude/commands/unfleet.md`** if present
(the command was removed at 3.2.0). New habitat flag `relay.escalate_after_k` defaults to 3 if unset.
The 3.2.5 AIterritory ASSESS-naming refinement rides this overwrite — but it is **forward-only: existing
free-counter `ASSESS_NN_<name>` assessments stay as-is (historical); the companion `ASSESS_<scriptnum>_<desc>`
+ `ASSESS_ADD_NN_<desc>` scheme applies to new assessments only. Do NOT bulk-rename.**

**Merge-class — never overwrite** (they carry instance config): `.claude/settings.json` and `.gitignore` —
add only the change a note names; never clobber the beast's `permissions.defaultMode` / `model` / custom
rules or its own ignores (`/reforge` §2). A vestigial `**Fleet:** member …` line in `guidance/02_derived.md`
→ update to a default-open note or drop it (preserve-class surgical edit).

**Non-idempotent — the `ASK.md` retirement (apply ONLY if coming from before 3.0.0):**
1. Ensure the habitat asks relay exists — `.habitat/asks/` + this beast's inbox + the `operator` inbox.
   (As of 3.2.3 the relay **self-bootstraps** on first delivery and `/habitat` scaffolds it — so this is automatic.)
2. Migrate each open `ASK.md` row into the relay: `kb-next`/`async` → the `operator` inbox; `beast:<X>` →
   X's inbox; `fyi`/trivial → drop (git keeps the trail). Carry the row's question + priority into the message.
3. Delete `ASK.md`. The relay + `/inbox` are the async-ask channel now (CLAUDE.md → *The asks relay*).

Then write `.template_version` to current and re-run `/fleet` to regenerate the registry cache in its current
(Member/Role-free) shape. Preserve all other project-instance files (`/reforge` §2). No data migration.

_Older than 2.2.0?_ Not shipped — re-spawn fresh from current, or obtain the full granular log
(`tracks/template_changelog_full.md`) from the maintainer. `/reforge` detects a below-window version and says so.
